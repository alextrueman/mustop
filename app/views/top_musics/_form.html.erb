<%= form_for(@top_music) do |f| %>
  <% if @top_music.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@top_music.errors.count, "error") %> prohibited this top_music from being saved:</h2>

      <ul>
      <% @top_music.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
